---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import type { Course } from '../data/courses';
import type { SiteLocale } from '../utils/paths';

interface Props {
  title?: string;
  description?: string;
  currentCourse?: Course | null;
  showPrimaryNav?: boolean;
  locale?: SiteLocale;
}

const {
  title = 'Learning Site',
  description = 'Structured learning materials for React, Spring Boot, and MySQL.',
  currentCourse = null,
  showPrimaryNav = true,
  locale = 'en',
} = Astro.props;
---

<!doctype html>
<html lang={locale === 'zh' ? 'zh-Hant' : 'en'}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>

  <body>
    <div class="site-shell">
      <Header currentCourse={currentCourse} showPrimaryNav={showPrimaryNav} locale={locale} />
      <main class="page-shell">
        <slot />
      </main>
      <Footer locale={locale} />
    </div>
  </body>
</html>
