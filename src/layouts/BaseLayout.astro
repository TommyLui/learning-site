---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import type { Course } from '../data/courses';
import type { SiteLocale } from '../utils/paths';

interface Props {
  title?: string;
  description?: string;
  currentCourse?: Course | null;
  showPrimaryNav?: boolean;
  locale?: SiteLocale;
}

const {
  title = 'Learning Site',
  description = 'Structured learning materials across frontend, backend, database, and systems tracks.',
  currentCourse = null,
  showPrimaryNav = true,
  locale = 'en',
} = Astro.props;
---

<!doctype html>
<html lang={locale === 'zh' ? 'zh-Hant' : 'en'}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=League+Spartan:wght@400;700;800;900&family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&family=Noto+Sans+TC:wght@400;500;700;900&display=swap" rel="stylesheet" />
    <title>{title}</title>
  </head>

  <body>
    <div class="site-shell">
      <Header currentCourse={currentCourse} showPrimaryNav={showPrimaryNav} locale={locale} />
      <main class="page-shell">
        <slot />
      </main>
      <Footer locale={locale} />
    </div>
  </body>
</html>
